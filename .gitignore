# OS / editor
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Node (only used for the optional engine.test.js sanity run)
node_modules/
npm-debug.log*
yarn-error.log*

# Logs / temp
*.log
tmp/
.cache/

# Local env / secrets — never commit
*.env
.env
.env.*
!*.env.example

# Unity drops this next to a WebGL export and names it after what to do with
# it. It is Burst's debug symbols — useless to a player, and 100 KB of noise in
# a repository whose whole job is being small enough to open instantly.
*_BurstDebugInformation_DoNotShip/
