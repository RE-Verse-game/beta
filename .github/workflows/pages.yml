name: Deploy to GitHub Pages

# Publishes the static browser build (repo root) to GitHub Pages on every
# push to main. No build step — the game is plain HTML/CSS/JS.
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Only one concurrent deploy; don't cancel an in-progress release.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  # Gate: a console that fails its own parity suite never reaches players.
  # Cheap enough to run on every deploy (no install step, no build).
  test:
    name: Engine tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node 2d/engine.test.js

  deploy:
    needs: test
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
        with:
          enablement: true   # auto-enable Pages (GitHub Actions source) if not set
      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .            # entire repo root is the site
      - id: deployment
        uses: actions/deploy-pages@v4
